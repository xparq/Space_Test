# Verify that the test runner will cd to test-case dirs

echo "Current dir: `pwd`"

#! Note: Spaces in the TC name WILL NOT survive this one, unfortunately:
SH ls -S "$CASE.*"

EXPECT "cd_to_parent_of_TC_file.case
"