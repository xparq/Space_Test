* The test exe has some non-default arg. arities defined, don't get fooled! ;)

* A few deliberately failing/crashing cases are disabled (prefixed with _) by
  default, in order for the GitHub autotest to succeed and not send me mails
  of failing jobs. ;)
